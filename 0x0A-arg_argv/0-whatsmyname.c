#include <stdio.h>

/**
 * main - function that writes its name
 * @argc: arg count
 * @argv: array of pointers to arg strings
 * Return: 0
 */
int main(int argc, char *argv[])
{
	argc = argc;
	printf("%\n", argv);
	return (0);
}
