#include "main.h"
/**
 * is_palindrome - is a function that returns a palindrome
 * @s: string value
 *
 * Return: 1 in success
 */
int is_palindrome(char *s)
{
	if (*s == '\0')
		return (1);
	_putchar('\n');
	return (0);
}
